# Discord Bot Token
DISCORD_TOKEN=

# Bot Owner ID - Your Discord User ID for receiving bot notifications (optional)
# To get your Discord User ID: Enable Developer Mode → Right-click your profile → Copy User ID
BOT_OWNER_ID=

# Path to the quotes file
QUOTES_FILE=data/quotes.yaml

# ------------------------------------------------------------------------------------
# Multi-guild operation is SAFE and NORMAL. ALLOWED_GUILD_IDS is optional and intended for development/testing only.
# In production, leave this unset to allow the bot to operate in all invited guilds.
# Security is provided by intelligent rate limiting and channel discovery, not by guild restriction.
#
# To restrict the bot to specific guilds during development/testing, add one or more Guild IDs, separated by commas.
# Example:
# ALLOWED_GUILD_IDS=1105309398705897633
#
ALLOWED_GUILD_IDS=

# Cleanup scan limit - The maximum number of recent messages to scan per channel.
# This enhances privacy and performance by avoiding deep scans of channel history.
# Use 'npm run cleanup -- --confirm' to run message cleanup (separate from bot startup)
CLEANUP_MAX_MESSAGES_PER_CHANNEL=100

# ------------------------------------------------------------------------------------
# Wandering Behavior Configuration
# These settings control how active the bot is and how it handles quiet servers
# ------------------------------------------------------------------------------------

# Minimum score threshold for channel selection (1-200, default: 30)
# Lower values = more active bot, higher values = more selective bot
# The original default was 50, but 30 provides better engagement
MIN_SCORE_THRESHOLD=30

# Loneliness bonus points per day for inactive servers (0-100, default: 15)
# Higher values = bot visits quiet servers sooner
# The original default was 5, but 15 provides better loneliness prevention
LONELINESS_BONUS_POINTS_PER_DAY=15

# Decision cycle interval in minutes (1-120, default: 10)
# How often the bot evaluates all guilds for potential messaging opportunities
# Lower values = more frequent checks, higher values = less frequent checks
DECISION_CYCLE_MINUTES=10

# ------------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------------

# Log level for debugging (debug, info, warn, error, default: info)
# Set to 'debug' to enable detailed channel discovery logging for troubleshooting
LOG_LEVEL=info